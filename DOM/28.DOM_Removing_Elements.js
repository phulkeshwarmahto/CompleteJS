// Removing Elements
// remove() - Remove an element from the DOM

// Removing a paragraph element
// let para = document.querySelector("p");
// para.remove(); // Removing the first paragraph element from the document
// console.log(para); // Logging the removed paragraph element (it still exists in memory)

// Removing a heading element
// let heading = document.querySelector("h1");
// heading.remove(); // Removing the heading element from the document
// console.log(heading); // Logging the removed heading element

// Note: When you remove an element, it is removed from the DOM tree but the object still exists in memory
// and can be re-inserted into the DOM if needed.
