// Working with Attributes
// getAttribute - Get attribute value
// setAttribute - Set attribute value
// removeAttribute - Remove attribute

// let div = document.querySelector("div")

// Getting attributes
// let id = div.getAttribute("id");
// console.log(id); // Logging the ID attribute value

// let name = div.getAttribute("name");
// console.log(name); // Logging the name attribute value

// Setting attributes
// let setId = div.setAttribute("id","newId");
// console.log(setId); // Setting a new id attribute for the <div> element

// let setName = div.setAttribute("name","newName");
// console.log(setName); // Setting a new name attribute for the <div> element

// Removing attributes
// div.removeAttribute("myP");
// // Removes the "myP" attribute from the <div> element, if it exists

// Practice with classList
// let para = document.querySelector("p"); // Selects the first <p> element

// para.getAttribute("myP"); 
// // Retrieves the value of the "myP" attribute, if it exists

// para.setAttribute("myP", "newclass"); 
// // Sets or updates the "myP" attribute of the <p> element to "newclass"

// para.classList.add("newclass"); 
// // Adds a class named "newClass" to the <p> element's class list

// para.classList.remove("para"); 
// // Removing the "para" class from the paragraph element

// para.classList.toggle("newclass");
// // Toggles the "newclass" class on the <p> element, adding it if it's not present, or removing it if it is    
