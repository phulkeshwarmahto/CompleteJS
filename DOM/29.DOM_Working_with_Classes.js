// Working with Classes
// classList.add() - Add a class to an element
// classList.remove() - Remove a class from an element
// classList.toggle() - Toggle a class on and off
// classList.contains() - Check if an element has a class

// Example 1: Add a class
// let para = document.querySelector("p");
// para.classList.add("newclass"); 
// // Adds a class named "newclass" to the <p> element's class list

// Example 2: Remove a class
// para.classList.remove("para"); 
// // Removing the "para" class from the paragraph element

// Example 3: Toggle a class
// para.classList.toggle("newclass");
// // Toggles the "newclass" class on the <p> element, 
// // adding it if it's not present, or removing it if it is

// Example 4: Check if element has a class
// if(para.classList.contains("newclass")) {
//     console.log("Element has the newclass class");
// }

// Example 5: Multiple operations
// let div = document.querySelector("div");
// div.classList.add("highlighted", "active"); // Add multiple classes
// div.classList.remove("highlighted"); // Remove a specific class
// div.classList.toggle("disabled"); // Toggle disabled class
